{{- if .Values.enableAppGin }}
apiVersion: v1
kind: Service
metadata:
  name: app-gin
spec:
  type: LoadBalancer
  ports:
    - port: 2096
      targetPort: 2096
  selector:
    app: app-gin
{{- end }}
